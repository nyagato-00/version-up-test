name: Auto Version Up

on:
  schedule:
    # 毎週月曜〜金曜の午前9時(UTC 0時)に実行
    - cron: "0 0 * * 1-5"

  # 手動実行用
  workflow_dispatch:

jobs:
  create-pr:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Fetch latest develop branch
        run: |
          git fetch origin develop
          git checkout develop
          git pull origin develop

      - name: Define branch name
        id: branch-name
        run: echo "branch_name=auto_versionup" >> $GITHUB_OUTPUT

      - name: Check if branch already exists
        id: check-branch
        run: |
          BRANCH_NAME=${{ steps.branch-name.outputs.branch_name }}
          if git ls-remote --exit-code --heads origin "$BRANCH_NAME"; then
            echo "Branch already exists: $BRANCH_NAME"
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "Branch does not exist: $BRANCH_NAME"
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Create or Checkout Branch
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          BRANCH_NAME=${{ steps.branch-name.outputs.branch_name }}

          if [ "${{ steps.check-branch.outputs.exists }}" == "true" ]; then
            git checkout "$BRANCH_NAME"
            git pull origin "$BRANCH_NAME"
            git merge develop --no-edit
          else
            git checkout -b "$BRANCH_NAME" develop
          fi

      - name: Install dependencies
        run: |
          if ! npm ci; then
            npm install
          fi

      - name: Version up
        id: version-up
        run: |
          # 現在のバージョンを取得
          CURRENT_VERSION=$(node -p "require('./package.json').version")
          
          # バージョンアップを実行
          npm version minor
          
          # 新しいバージョンを取得
          NEW_VERSION=$(node -p "require('./package.json').version")
          
          # 出力を設定
          echo "current_version=${CURRENT_VERSION}" >> $GITHUB_OUTPUT
          echo "new_version=${NEW_VERSION}" >> $GITHUB_OUTPUT

      - name: Push changes
        run: |
          git push origin ${{ steps.branch-name.outputs.branch_name }}

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          title: "Version Up to v${{ steps.version-up.outputs.new_version }}"
          body: |
            ## 自動バージョンアップPR
            
            現在のバージョン: v${{ steps.version-up.outputs.current_version }}
            新しいバージョン: v${{ steps.version-up.outputs.new_version }}
            
            ### 変更内容
            - `npm version minor` を実行
            - `package.json` を更新
            
            ### 注意事項
            - このPRはGitHub Actionsによって自動生成されています
            - マージ前に依存関係に問題がないか確認してください
          branch: ${{ steps.branch-name.outputs.branch_name }}
          base: develop
          delete-branch: false
          commit-message: "chore: bump version to v${{ steps.version-up.outputs.new_version }}"
